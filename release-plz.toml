[workspace]
# Default flow: publish to crates.io (Trusted Publishing, no token), then tag
# and create the GitHub release that carries the prebuilt binaries.
git_release_enable = true
git_tag_enable = true
changelog_update = true
dependencies_update = false
pr_labels = ["release"]
