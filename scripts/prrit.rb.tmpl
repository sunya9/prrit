# Generated by the release workflow of @REPO@; do not edit by hand.
class Prrit < Formula
  desc "Gerrit-style stacked pull requests for GitHub: one commit, one PR"
  homepage "https://github.com/@REPO@"
  version "@VERSION@"
  license "MIT"

  on_macos do
    on_arm do
      url "https://github.com/@REPO@/releases/download/v@VERSION@/prrit-aarch64-apple-darwin.tar.gz"
      sha256 "@SHA_DARWIN_ARM64@"
    end
    on_intel do
      url "https://github.com/@REPO@/releases/download/v@VERSION@/prrit-x86_64-apple-darwin.tar.gz"
      sha256 "@SHA_DARWIN_X86_64@"
    end
  end

  on_linux do
    on_arm do
      url "https://github.com/@REPO@/releases/download/v@VERSION@/prrit-aarch64-unknown-linux-musl.tar.gz"
      sha256 "@SHA_LINUX_ARM64@"
    end
    on_intel do
      url "https://github.com/@REPO@/releases/download/v@VERSION@/prrit-x86_64-unknown-linux-musl.tar.gz"
      sha256 "@SHA_LINUX_X86_64@"
    end
  end

  depends_on "gh"

  def install
    bin.install "prrit", "git-remote-prrit"
  end

  def caveats
    <<~EOS
      prrit drives GitHub stacks through the gh-stack extension:
        gh extension install github/gh-stack
    EOS
  end

  test do
    assert_match "one commit, one PR", shell_output("#{bin}/prrit --help")
  end
end
